# Admin credentials
ADMIN_EMAIL=admin@swayamgoyal.com
ADMIN_PASSWORD=replace-with-strong-password
ADMIN_NAME=Admin

# Session security (generate with: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))")
SESSION_SECRET=replace-with-64-hex-chars

# Database (for local development with better-sqlite3)
DATABASE_PATH=./src/lib/backend/db/swayam.sqlite

# App URL (used for redirects)
APP_URL=http://localhost:3000
